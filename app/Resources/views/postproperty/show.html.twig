{% extends 'base.html.twig' %}

{% block body %}
    <h1>PostProperty</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postProperty.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('postproperty_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('postproperty_edit', { 'id': postProperty.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
