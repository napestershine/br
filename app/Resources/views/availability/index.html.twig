{% extends 'admin.html.twig' %}
{% block header %} Availability {% endblock %}
{% block subheader %} Availability List {% endblock %}
{% block content %}
    <div class="row">
        <div class="col-md-1">
            <a href="{{ path('availability_new') }}" class="btn btn-success">Add New</a>
        </div>
    </div>
    <br/>
    <div class="table table-responsive">
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Sr. No.</th>
                <th>Type of Availability</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for availability in availabilities %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ availability.availabilityType }}</td>
                    <td>
                        <a class="btn btn-info" href="{{ path('availability_show', { 'id': availability.id }) }}">show</a>

                        <a class="btn btn-primary" href="{{ path('availability_edit', { 'id': availability.id }) }}">edit</a>

                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
