{% extends 'base.html.twig' %}

{% block body %}
    <h1>Availability list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Availabilitytype</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for availability in availabilities %}
            <tr>
                <td><a href="{{ path('availability_show', { 'id': availability.id }) }}">{{ availability.id }}</a></td>
                <td>{{ availability.availabilityType }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('availability_show', { 'id': availability.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('availability_edit', { 'id': availability.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('availability_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
