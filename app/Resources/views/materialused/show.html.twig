{% extends 'admin.html.twig' %}
{% block header %} Material Used{% endblock %}
{% block subheader %}Material Used Details{% endblock %}
{% block content %}



    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materialUsed.id }}</td>
            </tr>
            <tr>
                <th>Materialname</th>
                <td>{{ materialUsed.materialName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('materialused_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('materialused_edit', { 'id': materialUsed.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
