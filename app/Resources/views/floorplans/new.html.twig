{% extends 'base.html.twig' %}

{% block body %}


    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('floorplans_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
