{% extends 'admin.html.twig' %}
{% block header %}Area In {% endblock %}
{% block subheader %}Show Area Unit Details {% endblock %}
{% block content %}
    <div class="row">
        <div class="col-md-1">
            <a href="{{ path('areain_new') }}" class="btn btn-success">Add New</a>
        </div>
    </div>
    <br/>

    <div class="panel-body">
        <div class="row form-group">
            <label for="title" class="col-md-2">Area Unit Name</label>
            <div class="col-md-10">
                {{ areaIn.areaInName }}
            </div>
        </div>
        <br/>
        <div class="row">
            <div class="col-md-12">
                <div class="col-md-2">
                    <a class="btn btn-primary" href="{{ path('areain_index') }}">Back</a>
                    <a class="btn btn-info" href="{{ path('areain_edit', { 'id': areaIn.id }) }}">Edit</a>
                </div>
                <div class="col-md-1 pull-right">

                    {{ form_start(delete_form) }}
                    <input class="btn btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
