{% extends 'base.html.twig' %}

{% block body %}
    <h1>Area</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ area.id }}</td>
            </tr>
            <tr>
                <th>Areatype</th>
                <td>{{ area.areaType }}</td>
            </tr>
            <tr>
                <th>Calculatedarea</th>
                <td>{{ area.calculatedArea }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('area_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('area_edit', { 'id': area.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
