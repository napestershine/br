{% extends 'admin.html.twig' %}
{% block header %} Property Features and Amenities {% endblock %}
{% block subheader %} New Property Features and Amenities{% endblock %}
{% block content %}
    <div class="panel-body">
        <div class="form-horizontal">

            {{ form_start(form) }}
            <div class="form-group">
                <div class="col-md-3">Property Features and Amenities</div>
                <div class="col-md-6">
                    {{ form_widget(form.featureAmenName) }}
                </div>
            </div>
            <br/>
            <div class="form-group">
                <div class="col-md-2">

                    <input type="submit" value="Create" class="btn btn-success"/>
                    <a class="btn btn-primary" href="{{ path('propfeatureamen_index') }}">Back</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}