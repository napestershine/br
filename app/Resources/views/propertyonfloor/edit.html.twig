{% extends 'admin.html.twig' %}
{% block header %} Property on Floor {% endblock %}
{% block subheader %} Edit Property on Floor{% endblock %}
{% block content %}

    <div class="panel-body">
        <div class="form-horizontal">
            {{ form_start(edit_form) }}
            <div class="form-group">
                <div class="col-md-3">Edit Property Floor</div>
                <div class="col-md-6">
                    {{ form_widget(edit_form.propFloor) }}
                </div>
            </div>
            <br/>
            <div class="form-group">
                <div class="col-md-2">
                    <input class="btn btn-success" type="submit" value="Edit"/>
                    <a class="btn btn-primary" href="{{ path('propertyonfloor_index') }}">Back</a>
                </div>
                <div class="col-md-1 pull-right">
                    {{ form_start(delete_form) }}
                    <input class="btn btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </div>
            </div>
            {{ form_end(edit_form) }}


        </div>
    </div>
{% endblock %}