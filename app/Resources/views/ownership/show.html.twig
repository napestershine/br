{% extends 'admin.html.twig' %}
{% block header %} Ownership{% endblock %}
{% block subheader %} Ownership Details {% endblock %}
{% block content %}
    <div class="row">
        <div class="col-md-1">
            <a href="{{ path('ownership_new') }}" class="btn btn-success">Add New</a>
        </div>
    </div>
    <br/>

    <div class="panel-body">
        <div class="row form-group">
            <label for="title" class="col-md-2">Type of Ownership</label>
            <div class="col-md-10">
                {{ ownership.ownershipType }}
            </div>
        </div>
        <br/>
        <div class="row">
            <div class="col-md-12">
                <div class="col-md-2">
                    <a class="btn btn-primary" href="{{ path('ownership_index') }}">Back</a>
                    <a class="btn btn-info" href="{{ path('ownership_edit', { 'id': ownership.id }) }}">Edit</a>
                </div>
                <div class="col-md-1 pull-right">

                    {{ form_start(delete_form) }}
                    <input class="btn btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}