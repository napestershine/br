{% extends 'admin.html.twig' %}
{% block header %} Property Advertisement For {% endblock %}
{% block subheader %} Property Advertisement Purpose Details{% endblock %}
{% block content %}


    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ listPropFor.id }}</td>
            </tr>
            <tr>
                <th>Listpropforname</th>
                <td>{{ listPropFor.listPropForName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('listpropfor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('listpropfor_edit', { 'id': listPropFor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
