{% extends 'admin.html.twig' %}
{% block header %} Property Possesion On {% endblock %}
{% block subheader %} New Property Possesion Value{% endblock %}
{% block content %}

    <div class="panel-body">
        <div class="form-horizontal">

            {{ form_start(form) }}
            <div class="form-group">
                <div class="col-md-3">Property Possession On</div>
                <div class="col-md-6">
                    {{ form_widget(form.propPossesionOn) }}
                </div>
            </div>
            <br/>
            <div class="form-group">
                <div class="col-md-2">

                    <input type="submit" value="Create" class="btn btn-success"/>
                    <a class="btn btn-primary" href="{{ path('proppossesion_index') }}">Back</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

