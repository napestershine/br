{% extends 'base.html.twig' %}

{% block body %}
    <h1>PropertyAreaCategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ propertyAreaCategory.id }}</td>
            </tr>
            <tr>
                <th>Propareacategoryname</th>
                <td>{{ propertyAreaCategory.propAreaCategoryName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('propertyareacategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('propertyareacategory_edit', { 'id': propertyAreaCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
